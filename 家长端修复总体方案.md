# 家长端全面排查与修复 · 总体方案与落地总结

> 范围：家长端（前端 `frontend/src/pages/parent/*` + 后端 `backend/src/services|controllers|routes/parent*`）
> 状态：全部任务（#100–#107）完成；前端 `tsc -b`+`vite build` 通过，后端 `tsc` 通过，HTTP 冒烟全绿。

---

## 一、排查发现的问题与根因

| # | 现象 | 根因 |
|---|------|------|
| 1 | 背景/字体大部分白色 | Tailwind 配 `darkMode:'class'` 但**从不挂载 `.dark` 类** → 所有 `dark:` 变体失效，渲染出亮色 `bg-white`/`bg-slate-50`/`text-slate-900` |
| 2 | 任务无法查看详情、无法删除 | 缺任务详情页与路由；删除被 `status==='COMPLETED'` 硬阻断 |
| 3 | 学习报告页错误/关联错 | 详情页分数读取方式错 + 无「学科总任务 / 专项攻克」双版块 |
| 4 | 首页"待完成任务"显示 0 | 首页统计未正确聚合"进行中/未开始"任务 |
| 5 | 学情概览统计方式不清、结构乱 | 自带重复顶部导航（整页刷新）+ 统计口径未说明 |
| 6 | 学科档案页永远"请先绑定学员" | 取数把 `{children:[...]}` 当数组 → `list.length===0` |

---

## 二、修复方案与落地

### 后端（真实数据 + 越权防护）
- **parentDashboardService**：重写首页统计（子女数 / 待完成 / 待审批愿望 / 近 7 天报告），真实聚合任务状态。
- **parentTaskService**：放宽删除（COMPLETED 也可删，保留防误删护栏）；按亲子关系作用域过滤；详情含完整 include。
- **parentOverviewService**：重写学情概览（能力雷达内存索引、错误分布、学习连续、近 7 日趋势、KPI 总览、`methodology` 口径说明），无数据不伪造假雷达。
- **parentReportService**：报告双版块（category `SUBJECT_MAIN`/`SPECIAL`）过滤 + 详情版块标签 + 真实 `statistics` 分数。
- **越权防护**：`validateOwnership` 基于 `ParentChildRelation(ACTIVE)` 校验（IDOR 防护），与 children 同源一致。

### 前端（暗色统一 + 功能补全）
- **暗色统一**：全站硬编码 `#111722`(底)/`#232f48`(卡)/`#1a2332`(次底)/`#324467`(边)/`#3b82f6`(主) + 文字 `white`/`#92a4c9`/`#5b6b8c`；语义色转暗底（`bg-blue-500/15` 等）；图表 Canvas 改暗色；`Modal` 改暗色。**`dark:` 残留 0**。最后一页 `TaskConfigCenter`（建任务页，1905 行）本轮已脚本化改暗色。
- **任务管理**：新增 `TaskDetail` 页 + `/tasks/:id` 路由；删除按钮接入；列表显示状态/分类。
- **报告中心**：双版块切换 + 学科下拉；`ReportDetail` 重写（真实分数/时长 + 答题明细 + 版块标签）。
- **学情概览**：`StudentOverview` 重写（去重导航改用侧边栏、4 KPI 卡、近 7 日趋势柱、统计口径说明面板、快捷操作）。
- **学科档案**：修复 `children` 取数（`res.data.children` → `child.student.id`）+ 暗色 + `hideHeader`；共用 `SubjectLearningStateView` 对两端生效。
- **亲子管理 / 个人中心**：暗色 + 移除重复导航 + 学员快速入口（学情/档案带 `?studentId=`）。

---

## 三、验证结果（冒烟：parent1 / password123，端口 3000）

| 接口 | 结果 |
|------|------|
| LOGIN / CHILDREN | 200 |
| DASHBOARD STATS | 200，**pendingTasks=1**（原 bug「待完成=0」已修复） |
| OVERVIEW | 200，subjects=1、hasData=true、pendingTasks=1 |
| TASKS / TASK DETAIL | 200（含"冒烟答题任务"） |
| REPORTS（SUBJECT_MAIN / SPECIAL / all） | 200 |

**关键接口事实（前端取数务必遵守）**：
- `/parent/children` 返回 `{success,data:{children:[{relationId, student:{id,...}}]}}` —— 学员 id 在 `child.student.id`，不在顶层 `.id`。
- `/parent/overview` 是**路径参数** `GET /parent/overview/:studentId`（非 query）；前端 `StudentOverview` 调用正确。
- `/parent/reports` 需 `?studentId=`，缺则返回 400 `MISSING_STUDENT_ID`；`category` 仅收 `SUBJECT_MAIN`/`SPECIAL`。

---

## 四、遗留与建议
1. 测试库**报告数为 0**（`recentReports/totalReports=0`），需跑一次真实训练生成报告，以验证报告详情页与双版块 UI 在真实数据下的展示。
2. 存在 **6 条学科级 `agentDocument` INSTRUCTION** 冗余（与「科目指令配置」冲突），已令其失效并触发告警，是否删除待确认（勿擅自删，可能含独有内容）。
3. 建议前端对 `/parent/children` 返回形状统一封装为 `child.id = child.student.id`，避免后续页面再次踩坑。
