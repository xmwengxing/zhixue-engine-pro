# 任务删除逻辑优化说明

## 问题描述

当学员账户被管理员删除后，该学员关联的任务仍然存在，但由于任务状态可能是 `IN_PROGRESS`（进行中），导致家长无法删除这些"孤儿任务"。

## 优化方案

### 核心逻辑

**如果任务关联的学员已被删除，允许家长直接删除该任务，无需检查任务状态。**

理由：
1. 学员已删除，任务失去了实际意义
2. 学员无法继续完成任务
3. 保留这些任务会造成数据冗余
4. 家长应该有权清理这些无效任务

## 实现细节

### 后端修改

#### 1. 服务层 (`backend/src/services/parentTaskService.ts`)

**修改 `deleteTask` 方法：**

```typescript
async deleteTask(taskId: string, parentId: string) {
  // 查询任务时包含学员信息
  const task = await prisma.task.findUnique({
    where: { id: taskId },
    include: {
      student: true, // 包含学员信息
      trainingSessions: {
        select: {
          id: true,
          status: true,
        },
      },
    },
  });

  // 检查学员是否已被删除
  const studentDeleted = !task.student || task.student.status === 'DELETED';

  // 如果学员未被删除，执行正常的删除检查
  if (!studentDeleted) {
    // 检查任务状态
    if (task.status === 'IN_PROGRESS') {
      throw new Error('进行中的任务不能删除');
    }
    
    // 检查活跃训练会话
    const hasActiveSessions = task.trainingSessions.some(
      session => session.status === 'ACTIVE'
    );
    
    if (hasActiveSessions) {
      throw new Error('该任务有正在进行的训练会话，无法删除');
    }
  } else {
    // 学员已被删除，允许直接删除任务
    logger.info(`任务 ${taskId} 的学员已被删除，允许删除任务`);
  }

  // 执行删除
  await prisma.task.delete({
    where: { id: taskId },
  });
}
```

**删除检查流程：**

```
开始删除任务
    ↓
检查任务是否存在
    ↓
验证删除权限（创建者）
    ↓
检查学员状态
    ├─ 学员已删除 → 允许删除 ✅
    └─ 学员未删除
        ↓
    检查任务状态
        ├─ 进行中 → 拒绝删除 ❌
        └─ 非进行中
            ↓
        检查训练会话
            ├─ 有活跃会话 → 拒绝删除 ❌
            └─ 无活跃会话 → 允许删除 ✅
```

### 前端修改

#### 1. 任务列表页面 (`frontend/src/pages/parent/TaskList.tsx`)

**添加学员状态字段：**

```typescript
interface Task {
  id: string;
  title: string;
  mode: string;
  status: string;
  createdAt: string;
  student: {
    username: string;
    status?: string; // 添加学员状态字段
    studentProfile?: {
      realName: string;
    };
  };
}
```

**添加删除检查函数：**

```typescript
/**
 * 检查任务是否可以删除
 */
const canDeleteTask = (task: Task) => {
  // 如果学员已被删除，允许删除任务
  const studentDeleted = !task.student || task.student.status === 'DELETED';
  if (studentDeleted) {
    return true;
  }
  
  // 如果学员未被删除，只有非进行中的任务可以删除
  return task.status !== 'IN_PROGRESS';
};

/**
 * 获取删除按钮的提示文本
 */
const getDeleteTooltip = (task: Task) => {
  const studentDeleted = !task.student || task.student.status === 'DELETED';
  
  if (studentDeleted) {
    return '学员已删除，可以删除此任务';
  }
  
  if (task.status === 'IN_PROGRESS') {
    return '进行中的任务不能删除';
  }
  
  return '删除任务';
};
```

**优化删除按钮显示：**

```tsx
{canDeleteTask(task) ? (
  <button
    onClick={(e) => {
      e.stopPropagation();
      handleDeleteClick(task);
    }}
    className="text-red-600 hover:text-red-900"
    title={getDeleteTooltip(task)}
  >
    删除
  </button>
) : (
  <span 
    className="text-gray-400 cursor-not-allowed" 
    title={getDeleteTooltip(task)}
  >
    删除
  </span>
)}
```

**优化学员显示：**

```tsx
{task.student?.studentProfile?.realName || 
 task.student?.username || 
 '(学员已删除)'}
```

**删除确认弹窗提示：**

```tsx
{(!taskToDelete.student || taskToDelete.student.status === 'DELETED') && (
  <p className="text-sm text-orange-600 mt-2">
    ⚠️ 该任务的学员账户已被删除
  </p>
)}
```

## 功能特性

### 1. 智能删除检查

| 场景 | 学员状态 | 任务状态 | 是否可删除 | 说明 |
|------|---------|---------|-----------|------|
| 场景1 | 正常 | 待开始 | ✅ 可以 | 正常删除 |
| 场景2 | 正常 | 进行中 | ❌ 不可以 | 保护进行中的任务 |
| 场景3 | 正常 | 已完成 | ✅ 可以 | 正常删除 |
| 场景4 | 已删除 | 待开始 | ✅ 可以 | 清理孤儿任务 |
| 场景5 | 已删除 | 进行中 | ✅ 可以 | 清理孤儿任务 |
| 场景6 | 已删除 | 已完成 | ✅ 可以 | 清理孤儿任务 |

### 2. 用户体验优化

**删除按钮状态：**
- 学员已删除 + 任何状态 → 红色可点击按钮
- 学员正常 + 进行中 → 灰色禁用状态
- 学员正常 + 其他状态 → 红色可点击按钮

**提示信息：**
- 鼠标悬停显示详细提示
- 学员已删除：显示"学员已删除，可以删除此任务"
- 任务进行中：显示"进行中的任务不能删除"
- 正常情况：显示"删除任务"

**视觉反馈：**
- 学员列显示"(学员已删除)"
- 删除确认弹窗显示警告标识
- 橙色警告文字提示学员已删除

### 3. 日志记录

后端会记录特殊删除情况：

```
任务删除成功: <taskId>, 家长: <parentId> (学员已删除)
```

## 测试场景

### 测试1：删除学员后删除任务

**步骤：**
1. 创建家长账户和学员账户
2. 建立亲子绑定关系
3. 家长为学员创建任务
4. 管理员删除学员账户
5. 家长尝试删除任务

**预期结果：**
- ✅ 任务列表显示"(学员已删除)"
- ✅ 删除按钮可点击（红色）
- ✅ 点击删除弹出确认对话框
- ✅ 对话框显示警告"该任务的学员账户已被删除"
- ✅ 确认删除成功

### 测试2：正常学员的进行中任务

**步骤：**
1. 创建家长和学员账户
2. 创建任务并开始训练（状态变为 IN_PROGRESS）
3. 家长尝试删除任务

**预期结果：**
- ✅ 删除按钮禁用（灰色）
- ✅ 鼠标悬停显示"进行中的任务不能删除"
- ✅ 无法点击删除按钮

### 测试3：正常学员的已完成任务

**步骤：**
1. 创建家长和学员账户
2. 创建任务并完成训练
3. 家长尝试删除任务

**预期结果：**
- ✅ 删除按钮可点击（红色）
- ✅ 点击删除弹出确认对话框
- ✅ 确认删除成功

## 数据一致性

### 级联删除

删除任务时，以下关联数据会自动级联删除：

- ✅ 训练会话（TrainingSession）
- ✅ 训练报告（Report）
- ✅ AI 对话记录（AIConversation）
- ✅ 答题记录（Answer）

### 数据库约束

当前实现使用应用层检查，未来可以考虑在数据库层面添加约束：

```prisma
model Task {
  student User @relation("TaskAssignee", fields: [studentId], references: [id], onDelete: Cascade)
  creator User @relation("TaskCreator", fields: [createdBy], references: [id], onDelete: Restrict)
}
```

**说明：**
- `onDelete: Cascade` - 学员删除时自动删除任务
- `onDelete: Restrict` - 创建者（家长）删除时保护任务

## 安全性考虑

### 权限验证

- ✅ 只有任务创建者可以删除
- ✅ 通过 JWT token 验证用户身份
- ✅ 服务层二次验证任务所有权

### 数据保护

- ✅ 学员正常时，进行中的任务仍受保护
- ✅ 有活跃训练会话的任务仍受保护（学员正常时）
- ✅ 删除前需要用户确认

### 审计日志

- ✅ 记录所有删除操作
- ✅ 标记学员已删除的特殊情况
- ✅ 便于问题追踪和数据恢复

## 后续优化建议

### 1. 自动清理机制

添加定时任务，自动清理学员已删除的任务：

```typescript
// 每天凌晨执行
async function cleanupOrphanTasks() {
  const orphanTasks = await prisma.task.findMany({
    where: {
      student: {
        status: 'DELETED'
      }
    }
  });
  
  // 批量删除
  await prisma.task.deleteMany({
    where: {
      id: {
        in: orphanTasks.map(t => t.id)
      }
    }
  });
  
  logger.info(`清理孤儿任务: ${orphanTasks.length} 个`);
}
```

### 2. 批量删除功能

允许家长批量删除学员已删除的任务：

```tsx
<button onClick={handleBatchDeleteOrphanTasks}>
  批量清理已删除学员的任务
</button>
```

### 3. 数据库级联

修改 Prisma schema，在数据库层面处理：

```prisma
model Task {
  student User @relation("TaskAssignee", fields: [studentId], references: [id], onDelete: Cascade)
}
```

### 4. 软删除恢复

如果使用软删除，可以添加恢复功能：

```typescript
// 恢复学员账户时，同时恢复关联任务
async function restoreStudent(studentId: string) {
  await prisma.user.update({
    where: { id: studentId },
    data: { status: 'ACTIVE' }
  });
  
  // 任务会自动可用
}
```

## 相关文件

### 修改的文件
- `backend/src/services/parentTaskService.ts` - 优化删除逻辑
- `frontend/src/pages/parent/TaskList.tsx` - 优化前端显示和删除检查

### 相关文档
- `家长任务删除功能说明.md` - 删除功能总体说明
- `学号系统和用户删除功能修复报告.md` - 用户删除相关说明

## 总结

通过这次优化，解决了学员删除后任务无法清理的问题，提升了系统的数据一致性和用户体验。家长现在可以：

1. ✅ 删除学员已删除的任务（无论任务状态）
2. ✅ 清楚地看到哪些任务的学员已被删除
3. ✅ 获得明确的删除提示和反馈
4. ✅ 保持任务列表的整洁

同时保持了原有的安全机制：

1. ✅ 学员正常时，进行中的任务仍受保护
2. ✅ 权限验证机制不变
3. ✅ 数据级联删除机制不变
