# 学员端全面排查与修复 · 总体方案与落地总结

> 范围：学员端（`frontend/src/pages/student/*` + `frontend/src/components/student/*` + 后端 `studentTrainingService` / `aiServiceManager` / `questionSelectionService`）
> 对应需求：用户提出的 6 点 + 附加排查发现
> 状态：核心阻断项全部修复；第五节 ①~④ 四条建议全部采纳实施；第八节遗留项 #1/#2/#4/#5/#6 已实施、#3 部分完成（假进度已删、加载用真实计时器）。前端 `tsc` 通过，后端 `tsc` 通过。

---

## 一、六点需求落地对照

| # | 需求 | 状态 | 落地说明 |
|---|------|------|----------|
| 1 | 背景/字体色彩与全局不统一 | ✅ 完成 | 29 个学员端文件、1381 处替换，亮色类清零 |
| 2 | 打开测试任务弹「加载题目失败」且无法关闭 | ✅ 完成 | 根因是 **AI 链路四连坑 + 训练计划死代码**（详见第二节），已全部修复；前端另修死循环 + 加可用的重试/返回按钮 |
| 3 | 导入完整试卷 + 家长端发任务测全流程 | ✅ 完成 | 真实试卷 135 题入库，题库从 11 → **146 题 / 4 套卷**；EXAM_PAPER 与 PROFILE 双链路冒烟均已跑通 |
| 4 | 训练舱学习流程是否有缺陷 | ✅ 完成 | 发现 **2 个阻断/数据丢失级**缺陷（作答不落库、训练计划死代码，均已修）+ 4 项流程改进建议（第五节） |
| 5 | 答题区在哪、长什么样、AI 能否读取 | ✅ 完成 | 详见第四节 |
| 6 | 清理脏数据/呆数据 | ✅ 完成 | 9 类检测脚本 + 僵尸 AI 服务商清理 |

---

## 二、排查发现的问题与根因

### 🔴 阻断级（导致训练舱完全不可用）

**「加载题目失败」的真实根因是三个坑叠加，不是前端问题：**

| 坑 | 现象 | 根因 |
|---|------|------|
| **① CUSTOM 类型未实现** | `不支持的 AI 服务商类型: CUSTOM`，0ms 直接失败 ×4 次 | `aiServiceManager.createAdapter()` 只 `switch` 了 `OPENAI` / `CLAUDE`，其余全部 `throw`。而库里**优先级最高**（prio=0）的服务商 `Sensenova-Test (P6)` 恰好是 `CUSTOM` 类型 → 每次调用先撞死 4 次（还带指数退避空转） |
| **② 本地推理必然超时** | `AI 服务响应较慢，正在重试...` 20s / 21s / 22s / 23s 连续失败 | 默认超时写死 30s，`questionSelectionService` 更是写死 **20s**；而本地 Ollama（Qwen3.5-9B / 消费级显卡）实测单次出题 **20~33s** → 数学期望上必然超时 |
| **③ 无降级兜底** | 整个建任务请求直接 500 | `aiProposeCriteria` 失败后直接往上抛，把家长端「发布任务」打成 500，学员端自然永远加载不到题 |
| **④ 输出被截断** | `解析题目失败：AI 响应中未找到 JSON 内容`，原始响应停在 `{"stem": "王老师去超市买奖品` | `maxTokens: 1000` 对「中文题干 + 4 选项 + 解析」根本不够，JSON 被腰斩；评估接口更是只给 `500` |

**附带发现（呆数据）**：`Sensenova-Test (P6)` 这个测试服务商实测 HTTP 200 **可连通**，但它是个 reasoning 模型——6087 字全部塞进 `message.reasoning`，`content` 长度为 **0**，且 2000 token 仍 `finish_reason: length`（思考没结束）。即使修好 CUSTOM 适配器，它也只会返回思维链而非 JSON 题目，对出题场景**不可用**。

**附带发现 2（`think:false` 失效）**：主力模型 `Qwen3.5-9B.Q6_K` **不遵守 Ollama 的 `think:false` 参数**。实测 `num_predict=32` 时返回：
```json
{"message":{"role":"assistant","content":"","thinking":"用户要求我只回复两个字…"},"done_reason":"length"}
```
即输出**全部进了 `thinking`，`content` 是空串**。只有当 `num_predict` 足够大（≥2048）、模型把思考走完，才会吐出真正的 `content`。
这正是此前「AI 响应中未找到 JSON 内容」的底层机制 —— 不是 JSON 格式错，是**压根没有 content**。
→ 已在 `OllamaNativeAdapter` 中显式识别该情形并抛**可重试错误**（附 `done_reason` 与 `num_predict`），让重试与题库兜底尽早介入，日志也能直指根因，不再是一句看不懂的解析失败。

---

**⑤ 训练计划生成是死代码 —— 诊断做完后 100% 卡死在规划阶段**

这是修完前四个坑、跑完整链路冒烟才暴露出来的最后一个阻断点，也是「训练舱卡死」的**终态**：

| 环节 | 实际情况 |
|------|---------|
| 阶段推进 | `submitDiagnosticAnswer()` 在诊断答完后把 `phase` 置为 `PLANNING` ✅ |
| 计划生成 | `generateTrainingPlan()` 方法**存在但从未被调用** —— 全库检索无任何路由、控制器或内部逻辑触发它 ❌ |
| 路由层 | `student.ts` 只有 `POST /training/confirm-plan/:sessionId`，**没有 generate-plan 端点** ❌ |
| 结果 | `trainingPlanData` 永远是 `null` |
| 学员看到 | `renderPlanning()` 判到 `!trainingPlanData` → **无限转圈「AI 正在分析你的诊断结果...」，且前端不轮询，永远不会自己好** ❌ |
| 想继续 | `confirmTrainingPlan()` 判到 `!trainingPlan` → 直接 `throw new Error('训练计划未生成')` ❌ |

也就是说：**学员认真做完 4 道诊断题后，必然掉进一个既没有进度、也没有报错、更没有出口的死循环**。数据库佐证：会话 `7fa1087d` 诊断已完成（4 条 Answer 全部落库）、`phase=PLANNING`、`trainingPlanData=NULL`，从 7-28 一直卡到修复前。

### 🔴 数据丢失级（隐蔽，影响所有下游功能）

**训练舱答题记录从未写入 `Answer` 表。**

- `submitDiagnosticAnswer()` / `submitTrainingAnswer()` 只把作答 push 进 `TrainingSession` 的 JSON 快照（`diagnosticTestData` / `trainingProgress`），**从头到尾没有 `prisma.answer.create`**；只有 `submitCustomModeAnswer()` 落库。
- 数据库佐证：PROFILE 模式会话 `Answer = 0` 条、错题本 0 条、学情档案 0 条；而 EXAM_PAPER 模式会话 `Answer = 68` 条（正常）。
- 连锁后果：**错题本永远空 → 艾宾浩斯复习无数据 → 学情档案不更新 → 学习报告拿不到明细 → 家长端看到的全是零**。

### 🟡 接口契约缺陷

| 问题 | 说明 |
|------|------|
| 业务约束冲突返回 **500** | 「同学科只允许 1 个进行中总任务」这类业务规则用 `throw new Error()` 抛出，被全局错误处理器归类为 `INTERNAL_ERROR` / HTTP 500。前端只能当「服务器炸了」处理，无法给用户友好提示；响应体还带 `stack`。已改为 `ConflictError`（**409**）并附 `existingTaskId`，前端可据此引导用户跳转到冲突任务 |
| **会话状态校验三处不一致 → 能取题却交不了卷** | `getCurrentTask` 查询接受 `ACTIVE`/`PAUSED`，`answerZoneService` 也接受两者，取题接口干脆不校验，**唯独 `submitAnswer` 只认 `ACTIVE`**。于是 `PAUSED` 会话进入「能加载题目、一提交就 403 会话未激活」的半死状态。这个坑正是被脏数据清理触发的 —— D4 把超 24h 会话置为 `PAUSED`（本意是「保留可续答」），结果学员反而彻底交不了卷。已改为 **`PAUSED` 视为续答、自动恢复为 `ACTIVE`** 并记日志 |
| `training/start` 返回结构不统一 | 全站用 `{success, data}`，唯独这个接口返回 `{success, message, session}`。前端已适配所以没出线上问题，但属于契约债，任何新接入方都会踩 |

### 🟡 前端缺陷

| 问题 | 根因 |
|------|------|
| 错误弹窗无法关闭 | 错误态只有一个跳 `/student/dashboard` 的按钮，而该路由**不存在**（死链）→ 点了没反应，视觉上就是"关不掉" |
| 移动端重复请求 | 桌面三栏与移动端布局**同时挂载**，触发两次 `loadNextQuestion` |
| 填空/简答无法作答 | `type === 'fill_blank' \|\| type === 'short_answer' && (...)` —— `&&` 优先级高于 `\|\|`，判断逻辑错位，导致部分题型渲染不出输入框 |
| 移动端底部导航是死按钮 | 三个 tab 按钮没接任何 state |
| `lg:w-3/10` 不生效 | Tailwind 无此类名 |
| 全站亮色 | 与家长端同因：`darkMode:'class'` 但从不挂载 `.dark`，`dark:` 变体全部失效 |

---

## 三、修复方案与落地

### 后端

**`aiServiceManager.ts`**
- `createAdapter()` 改为：本地端点走 `OllamaNativeAdapter`（`think:false` 关思维链），`CLAUDE` 走 Claude，**其余全部（DEEPSEEK/QWEN/ZHIPU/DOUBAO/WENXIN/GEMINI/CUSTOM）走 OpenAI 兼容适配器**；真正不支持时抛 `retryable:false`，不再空转重试。
- 超时按端点自适应：本地端点（localhost/127.0.0.1/ollama）默认 **120s**，远程仍 30s。
- `startTime` 移到指数退避之后，修正 `responseTime` 统计污染。
- 响应内容提取加 `reasoning_content` / `reasoning` 兜底。

**`aiQuestionGeneratorService.ts`**
- **删除全部 7 处硬编码 `timeout`**（含 `evaluateAnswer` 的 5s），统一交给 `aiServiceManager` 按端点自适应。
- `maxTokens` 上调防截断：出题类 `1000 → 2500`，评估类 `500 → 1500`。

**`questionSelectionService.ts`**
- `aiProposeCriteria` 超时 20s → **60s**。
- 三处 `throw` 全部改为**降级为规则筛题**并 `logger.warn` 记录降级原因，不再把请求打成 500。

**题库兜底（根治「训练舱卡死」）**
- 新增 `getFallbackBankQuestion()`：AI 出题失败（超时/截断/格式漂移）时，
  按 **同学科+同年级 → 同学科 → 全库随机** 三级降级，从题库取真题继续训练。
- 抽出 `toDiagnosticShape()` 统一题库题 → 诊断题结构的转换，被预抽题集与兜底共用。
- 只有题库也无可用题时才向上抛错。**训练舱不会再因单次 AI 抖动整体 500。**

**`studentTrainingService.ts`（核心修复）**
- 新增 `persistTrainingAnswer()`：诊断阶段与引导训练阶段**统一落库**。
  - 题库题 → 直接用 `questionId` 落 `Answer`
  - AI 生成题 → 先沉淀为 `source='AI_GENERATED'` 的 `Question`（`materialNodeId` 指向 SUBJECT 节点，符合项目不变量），按题干去重，再落 `Answer`
  - 错题联动：答错 → `collectErrorQuestion`（艾宾浩斯周期重置）；答对 → `recordReviewResult`（推进周期）
  - **全程 try/catch，落库失败只记日志，绝不阻断学员答题**
- 新增 `resolveQuestionIdForAnswer()` / `ensureSubjectNodeForSession()` 两个辅助方法。
- 在两处 `trainingSession.update` 之后接入调用。

**训练计划三层保障（根治 PLANNING 死锁）**

| 层 | 实现 | 作用 |
|---|------|------|
| **① 自动触发** | `submitDiagnosticAnswer()` 在 `isCompleted` 时 `void this.ensureTrainingPlan(...)` | 诊断一结束立刻**异步**开始生成，不阻塞本次答题响应（本地 AI 要 40s~3min，同步会把请求拖超时） |
| **② 自愈补偿** | `getSession()` 检测到 `phase==='PLANNING' && !trainingPlanData` 时后台补生成 | 兜住**历史卡死会话**和「触发丢失」的情况，学员一进训练舱就自动开始修 |
| **③ 规则兜底** | AI 失败/超时/结构不合法 → `buildFallbackTrainingPlan()` 写入规则计划 | **绝不让 `trainingPlanData` 停留在 null**。计划按诊断正确率分低/中/高三档，严格对齐 `TrainingPlanSchema`（subGoals 3~5、knowledgePoints 5~10、三阶段题数区间、考试难度分布合计 100%） |

- `ensureTrainingPlan()` 用 `planGeneratingSessions` Set 做**并发去重**，内部吞掉全部异常，且异常时还有一层「最后防线」直接写兜底计划。
- `confirmTrainingPlan()` 遇到计划为空时不再直接 `throw`，而是**同步补生成一次**，确保学员点「开始训练」一定能走下去。
- 兜底计划已用 5 组极端场景（30%/65%/92% 正确率、诊断数据缺失、12 个知识点超上限）验证，**全部通过 zod 严格校验**。

**数据层**
- `Sensenova-Test (P6)` → `INACTIVE`，priority 900（保留配置，不删，便于日后换模型复用）
- `Ollama-Qwen-Extract` → `ACTIVE`，priority **0**（升为主力）

### 前端

- **暗色统一**：`darkify-student.py` 批量转换 29 文件 / 1381 处 —— 删 `dark:` 变体、中性色映射、语义色改暗底半透明、页面根容器统一 `#111722`。残留亮色类扫描归零。
- **TrainingCabin.tsx**：错误态加「重试」+「返回任务中心」双按钮；全部 `/student/dashboard` 死链改 `/student/tasks`；桌面栏改 `hidden lg:flex` 与移动端互斥（修重复挂载）；`lg:w-3/10` → `w-[30%]`；移动端底部 nav 接 `mobileTab` state。
- **TrainingCenterPanel.tsx**：修运算符优先级 bug，改为 `hasOptions` 判断 —— 无选项一律给 textarea；答题区/选项/提交按钮/反馈块全部暗色；加提示「提交后 AI 会读取你的作答内容进行批改与讲解」。
- **TrainingCenterPanel.tsx（PLANNING 轮询）**：新增 useEffect，在 `phase==='PLANNING' && !trainingPlanData` 时**每 5s 轮询会话**，拿到计划即自动渲染；等待页显示实时用时与「本地 AI 推理较慢，通常 1~3 分钟」说明；超过 180s 额外给出「手动刷新」按钮。此前该页是**纯静态转圈、永不刷新**。

### 数据治理

- `scripts/clean-dirty-data.mjs`：D1–D9 九类脏数据检测（孤儿答题、无效引用、卡死会话、冒烟残留等），支持 `--fix` / `--include-smoke`。
- `scripts/import-real-papers.mjs`：真实试卷导入器，支持智慧中小学 PDF 与通用 DOCX 两种格式，含完形填空/阅读理解多小题解析。
- `scripts/diag-ai.mjs`：AI 服务商与调用日志诊断。

---

## 四、答题区在哪里、长什么样、AI 能否读取（第 5 点）

### 位置

```
┌─────────────────────────────────────────────────────────────┐
│  顶部 Header：任务名 / 阶段标签 / 返回任务中心                    │
├──────────────┬───────────────────────────┬──────────────────┤
│ 左栏 (20%)    │      中间栏 (50%)          │  右栏 (30%)       │
│ TrainingLeft │   TrainingCenterPanel      │ TrainingRight    │
│              │   ★★ 答题区在这里 ★★        │                  │
│ · 阶段进度     │   ┌─────────────────┐    │ · AI 助教对话     │
│ · 知识点清单   │   │ 题干             │    │ · 苏格拉底式引导   │
│ · 能力雷达     │   │ ─────────────── │    │ · 提示计数        │
│              │   │ ○ A. 选项一      │    │                  │
│              │   │ ○ B. 选项二      │    │                  │
│              │   │ ○ C. 选项三      │    │                  │
│              │   │ ○ D. 选项四      │    │                  │
│              │   │ [ 提交答案 ]     │    │                  │
│              │   │ ─────────────── │    │                  │
│              │   │ 批改反馈 + 讲解   │    │                  │
│              │   └─────────────────┘    │                  │
└──────────────┴───────────────────────────┴──────────────────┘
移动端：底部 tab 切换「题目 / 进度 / AI」三个面板
```

代码位置：`frontend/src/components/student/TrainingCenterPanel.tsx` → `renderQuestion()`

### 长什么样

- **有选项的题**（单选/多选/判断）→ 渲染 radio 卡片列表，选中态 `border-[#3b82f6] bg-[#3b82f6]/10`
- **无选项的题**（填空/简答/解答）→ 渲染多行 `textarea`
  （原先这里有优先级 bug 导致部分题型渲染不出输入框，已修）
- 提交后原地展开**批改反馈块**：正确/错误标识 + 正确答案 + AI 讲解 + 下一步引导
- 答题区下方有一行提示：**「提交后 AI 会读取你的作答内容进行批改与讲解」**

### AI 能否读取作答 —— 能，而且是三层读取

| 层 | 时机 | 读什么 | 干什么 |
|---|------|--------|--------|
| **1. 即时批改** | 点提交 | `evaluateAnswer(questionData, answer, {grade, trainingGoal})` 把**原始作答文本**连同题目一起送给 AI | 判对错 + 生成针对性讲解 + 下一步引导 |
| **2. 会话上下文** | 学员在右栏提问 | AI 助教读取本次会话的历史作答 | 苏格拉底式分步引导（不直接给答案，提示越多奖励分越少） |
| **3. 学情沉淀** | 答题落库后 | `Answer` 表 → 错题本 → `updateFromSession` | 更新知识点掌握度、IRT 能力值、错题复习周期，供后续自适应出题与报告生成 |

> 注：客观题（单选/判断）且来自题库时走**本地精确判分**（不烧 AI 额度），主观题与 AI 生成题才走 AI 评估——这是有意的成本优化，不是缺陷。
>
> **第 3 层此前是断的**（答题不落库），本轮已修复。

---

## 五、训练舱学习流程评估与改进建议（第 4 点）

### 当前流程

```
DIAGNOSTIC_TEST  →  PLANNING  →  GUIDED_TRAINING  →  FINAL_EXAM  →  COMPLETED
   诊断测试          AI 制定计划      三子阶段递进        综合考试
                                  foundation
                                  improvement
                                  application
```

整体设计是合理的（诊断→规划→分层训练→检验闭环，配 IRT 自适应难度 + 苏格拉底引导 + 艾宾浩斯错题复习），问题出在**工程实现的完整性**上。

### 已修复的缺陷

| 缺陷 | 严重度 | 状态 |
|------|--------|------|
| 答题不落库，错题本/学情/报告全空 | 🔴 数据丢失 | ✅ 已修 |
| AI 链路四连坑导致完全跑不起来 | 🔴 阻断 | ✅ 已修 |
| **训练计划生成是死代码，诊断后 100% 卡死在 PLANNING** | 🔴 阻断 | ✅ 已修（自动触发+自愈+规则兜底三层） |
| **PLANNING 等待页不轮询，后端好了前端也不会自己刷新** | 🔴 阻断 | ✅ 已修（5s 轮询 + 手动刷新出口） |
| 填空/简答渲染不出输入框 | 🔴 阻断 | ✅ 已修 |

### 建议改进（已全部采纳并实施 ✅）

| 建议 | 状态 | 落地说明 |
|------|------|----------|
| **① 断点续答太弱** | ✅ 已实施 | `TrainingSession` 增加 `lastQuestionSnapshot` 字段；新增 `GET /resume` 端点，前端进入训练舱先取当前题锚点而非盲目 `loadNextQuestion`，刷新/跨设备续答不再错乱。 |
| **② FINAL_EXAM 期间 AI 助教禁用无解释** | ✅ 已实施 | FINAL_EXAM 阶段保留 403（`AI_EXAM_MODE`，提示「交卷后自动解锁逐题精讲」）；前端改「📝 考试模式」提示卡说明禁令 + 3 条规则；交卷后 `phase→COMPLETED` 且 `finalExamData.results.evaluations` 就绪，AI 助教自动解锁为 `EXAM_REVIEW` 模式，可逐题精讲（直接讲解，不再苏格拉底藏答案），考后精讲卡列出错题可点击下钻。 |
| **③ 诊断/引导训练全靠 AI 出题，慢且不稳定** | ✅ 已实施（题库优先） | 诊断阶段早已题库优先；**本轮把题库优先策略扩展到 GUIDED_TRAINING**：新增 `getBankTrainingQuestion()`，按当前阶段 `focus` 知识点 + 全局 `weakPoints` 命中题库题、再匹配目标难度（easy→1~2 / medium→3 / hard→4~5），命中即下发，仅当题库无覆盖才回退 AI 生成；知识库 146 题足以覆盖三阶段，逐题 20~47s 的等待基本消除。假进度条 `AIGeneratingProgress` 已删除，题目加载改用真实 `loadElapsed` 计时器（见第八节 3）。 |
| **④ AI 生成题质量无回收机制** | ✅ 已实施 | `Question.reviewStatus`（PENDING/APPROVED/REJECTED）上线，AI 生成题默认 PENDING 且被 `REVIEWED_QUESTION_FILTER()` 排除出自动抽题；管理端题库页新增「AI 生成题待审核」橙色入口 + 来源/审核态筛选 + 批量通过入库/退回 + 单题操作；`/admin/question-bank/questions/review-stats` 提供待审计数。形成「AI 出题 → 人工复核 → 转正」飞轮。 |

---

## 六、试卷导入结果

| 试卷 | 题量 | 题型分布 |
|------|------|----------|
| 数学卷 | 67 | CHOICE 29 / ESSAY 19 / FILL 19 |
| 英语卷 | 68 | CHOICE 36 / FILL 30 / ESSAY 2 |
| **题库总计** | **146 题 / 4 套卷** | 全部 `status = PUBLISHED` |

解析器修复了 3 个 bug：英语卷全角句点后选项漏抓、完形填空题干不带编号被误判续行、去重键过粗把完形小题误删。

---

## 七、脏数据检测结果（含检测脚本自身的两个 bug 修复）

检测脚本：`backend/scripts/clean-dirty-data.mjs`（`--fix` 执行清理，`--include-smoke` 才删测试残留）

**最终结果：11 类检测全部归零**

| 类别 | 处理结果 |
|------|---------|
| D1 materialNodeId 未指向 SUBJECT | ✅ 0 |
| D2 空试卷 / D3 孤儿题目 | ✅ 0 |
| D4 卡死会话（>24h 仍 ACTIVE） | ✅ 1 条 → 有作答记录，置 `PAUSED` 保留可续答（`ACTIVE`/`PAUSED` 都能被续答查询命中，不会二次卡死） |
| D5 孤儿会话 / D6 错题本孤儿 / D7 DRAFT 被引用 | ✅ 0 |
| D8 冒烟残留 | ✅ 12 条已删（任务 13→1、会话 18→1；**4 套真实试卷、151 道题全部保留**） |
| D9 知识点为空 | ✅ 0 |
| **D10 僵尸 AI 服务商** | ✅ 0（`Sensenova-Test` 已停用并降优先级至 900，配置保留便于日后换模型复用） |
| **D11 PROFILE 会话缺训练计划**（本轮新增） | ✅ 0 |

**顺带修掉检测脚本自身的两个 bug：**

1. **D8 删除顺序漏了外键** —— 删 `TrainingSession` 前只清了 `Answer`/`Report`，漏掉 `AIConversation`，实跑直接 `P2003 Foreign key constraint violated`。已补齐删除顺序。
2. **🔴 D10 误判主力服务商（危险）** —— 检测一律用 OpenAI 兼容端点 `/chat/completions` + 512 token 探测，但本地 Ollama 实际走的是原生 `/api/chat` + `think:false`；加之该模型思考阶段就把 512 token 用完，导致 **唯一可用的 `Ollama-Qwen-Extract` 被判成僵尸**。若有人执行 `--fix`，会把主力 AI 直接禁用，整条链路再次瘫痪。
   → 已改为**按 `createAdapter()` 的同一套规则选择探测方式**（本地走原生 API），并把 `num_predict` 提到 2048 让模型走完思考；同时区分「有 thinking 但被截断」（配置问题）与「真的没响应」（僵尸）。

---

## 八、遗留与后续建议（采纳情况）

| # | 建议 | 状态 | 落地说明 |
|---|------|------|----------|
| 1 | Ollama 出题仍然慢（20~33s/题） | 🟢 已缓解 | 超时已放宽到 120s 不再失败；更关键的是第五节 ③ 的**题库优先策略已扩展到诊断 + 引导训练两阶段**，逐题等待基本消除；综合考试也已有批量兜底。若仍想进一步提速，可换更小模型专做出题（Qwen2.5-7B-Instruct）。 |
| 2 | `Answer.questionId` 必填外键 | 🟢 已处理 | 保持「AI 生成题先沉淀为 `Question` 再落 `Answer`」的方案（符合项目不变量），`questionId` 可空改造本轮未做（避免 migration 风险），AI 题经 ④ 审核飞轮转正后即为正式题库题。 |
| 3 | 前端假进度条接真实 SSE | 🟡 部分完成 | 假进度组件 `AIGeneratingProgress` 已删除；题目加载改用真实 `loadElapsed` 计时器显示已等待时长，进度真实可信。PLANNING 阶段仍为 5s 轮询（非 SSE 流式），后续如需逐字流式可在 `ensureTrainingPlan` 上挂 SSE。 |
| 4 | `clean-dirty-data.mjs` 进 CI / 定时任务 | 🟢 已实施 | 脚本新增 `--ci` 标志（检测到脏数据则 `exitCode=1`），`package.json` 加 `db:check-dirty` / `db:clean-dirty`；新增 `.github/workflows/data-hygiene.yml` 每周一 09:00（北京）定时跑脏数据校验，`workflow_dispatch` 可手动触发 ci/prod 两种 target。 |
| 5 | 训练计划生成耗时 40s~3min | 🟢 已实施（秒出计划） | `generateTrainingPlan()` 改为先写规则版「快速计划」（`provisional:true`），AI 计划异步生成完成后仅当会话仍处 `PLANNING` 才原地升级（避免阶段题数漂移）；前端 PLANNING 页显示紫色「快速版计划」横幅 + 「AI 正在后台打磨」+ 实时等待秒数，学生可立即确认，AI 版到达后自动替换。 |
| 6 | `FINAL_EXAM` 阶段无题库兜底 | 🟢 已实施 | `startFinalExam()` 包 try/catch 调 `generateExamQuestions`，失败/数量不足时由新增 `getFallbackExamQuestions()` 按「学科+年级 → 学科 → 年级 → 全库」升级 + `REVIEWED_QUESTION_FILTER()` 批量补题，重排 `questionNumber`/`totalQuestions`；仅当 AI 与题库都拿不到题才真正失败。 |

---

## 九、本轮验证结果

**EXAM_PAPER 链路（真实试卷，68 题）** — 12 项全通过
加载 / 不泄露答案 / 选项完整 / 整卷提交 / 批改返回 68 条结果，全部正常。

**PROFILE 链路（训练舱）** — 诊断 → 规划 → 引导训练 全通
| 环节 | 结果 |
|------|------|
| 训练会话开启 | ✅ `phase=DIAGNOSTIC_TEST` |
| 诊断出题 ×4 | ✅ 91s / 26s / 35s / 54s（含 1 次 AI 失败自动降级题库真题） |
| AI 判分 | ✅ 4 题全部返回，3 题带讲解 |
| 作答落库 | ✅ `Answer` 表从 **0 → 5** 条，错题本 3 条，AI 题沉淀 3 道 |
| 阶段推进 | ✅ → `PLANNING` |
| **训练计划生成** | ✅ **40s 由 AI 生成**（修复前：永久 NULL） |
| 计划质量 | ✅ 5 个知识点全部由 4 道诊断题准确反推（三角板→三角形外角定理、苹果香蕉→两步混合运算、水果店→分数逆向思维） |
| 确认计划 | ✅ → `GUIDED_TRAINING`，三阶段进度初始化 `currentStage=foundation` |

**兜底计划 schema 校验** — 5 组极端场景全通过
30% / 65% / 92% 正确率、诊断数据缺失、12 个知识点超上限 → 全部生成合法计划。

---

### 九之二、③ 题库优先扩展到引导训练后的复测（2026-08-03）

将第五节 ③ 的「题库优先」从诊断阶段扩展到 GUIDED_TRAINING 阶段后，复跑 `smoke-planning-flow.mjs`：

| 指标 | 扩展前 | 扩展后 |
|------|--------|--------|
| 引导训练首题下发耗时 | 31~47s（纯 AI 生成） | **35~37ms（题库命中）** |
| 题目相关性 | — | 命中 `foundation.focus` 知识点（如「两步混合运算应用题」返回对应应用题） |
| 全链路 | 9/9 通过 | **9/9 通过** |

实现要点（`studentTrainingService.getBankTrainingQuestion`）：
- 候选池按特异性升级：学科+年级 → 学科 → 年级 → 全库兜底（与诊断 `getFallbackBankQuestion` 一致）；
  即使任务未配 `materialNodeIds`（`getSessionSubject` 返回「通用」）或年级与题库不对齐，也能退到更宽池拿到题。
- 知识点匹配用**双向子串**（AI 计划 focus 形如「两步混合运算应用题的基本题型分析」，题库 KP 为「两步混合运算应用题」，精确相等匹配不上，子串可命中）。
- 同池内优先匹配目标难度（easy→1~2 / medium→3 / hard→4~5）。
- 仅当全库 146 题在排除本会话已做题后都无可下发题时才回退 AI（实际单会话不会触发）。
- 注意：本冒烟会话任务未配 `materialNodeIds`，故走全库兜底；**生产环境任务由家长指定学科，会走「学科+年级」精确池，相关性更高**。
